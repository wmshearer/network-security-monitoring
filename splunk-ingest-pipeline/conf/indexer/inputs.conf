# Enables the indexer to receive data forwarded from the Universal Forwarder
# at /home/kali/splunkforwarder. 9997 is Splunk's documented default cooked
# TCP receiving port (forwarder -> indexer, s2s protocol, not raw syslog).
[splunktcp://9997]
disabled = false
