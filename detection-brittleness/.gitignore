# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Zircolite's own default log (redundant with evidence/zircolite_raw/*.log.txt,
# which are the named, per-run evidence files this project actually cites)
zircolite.log

# Derived/staged sample data: copies or reformatted copies of files that live
# in _corpora/, regenerated by scripts/01_prepare_samples.py. Never the
# source of truth; the manifest + scripts describe how to rebuild them.
evidence/samples/

# Zircolite's compiled SQLite databases if ever produced on disk
*.db
