# Converted event data is regenerated by src/convert_evtx.py / src/convert_otrf.py
# from source data that lives outside this repo (ai-triage-engine/data). Keep the
# repo itself small; evidence/ (small, curated outputs) IS tracked, data/converted/
# (full converted corpora, hundreds of MB) is NOT.
data/converted/
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
