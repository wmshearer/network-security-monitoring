# Provisioned Grafana datasource - loads automatically on container start,
# no manual "Add data source" click required.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
